using System.Reflection.Metadata.Ecma335;

namespace KittyTask
{
    public class KittyTask1
    {
        /* Convert minutes to seconds and assign the result to variable 'seconds'.
         * Assume that input is a whole number.
         */
        public int Invoke(int minutes)
        {
            var seconds = 0;
            seconds = minutes * 60;






            return seconds;
            
        }
    }
}
